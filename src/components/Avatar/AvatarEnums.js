// AvatarSize enum used by the Avatar size prop
export const AvatarSize = Object.freeze({
    XS: 'xs',
    SM: 'sm',
    MD: 'md',
    LG: 'lg',
});

// AvatarTitleSize enum used by the AvatarTitle size prop
export const AvatarTitleSize = Object.freeze({
    SM: 'sm',
    LG: 'lg',
});

/* global describe */
import * as common from '../../../test/unit/commonTests';
import TabsPane from './TabsPane';

describe('TabsPane', () => {
    common.allowsDataProps(TabsPane, { tagName: 'section' });
});

// TextFieldVariant enum used by the TextField variant prop
export const TextFieldVariant = Object.freeze({
    DEFAULT: 'default',
    TITLE: 'title',
});

// TextAreaAutoHeight enum used by the TextArea autoHeight prop
export const TextAreaAutoHeight = Object.freeze({
    ENABLED: true,
    DISABLED: false,
});

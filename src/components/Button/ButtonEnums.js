// ButtonIconPosition enum used by the Button iconPosition prop
export const ButtonIconPosition = Object.freeze({
    LEFT: 'left',
    RIGHT: 'right',
});

// ButtonType enum used by the Button type prop, matches html button type attribute
export const ButtonType = Object.freeze({
    BUTTON: 'button',
    RESET: 'reset',
    SUBMIT: 'submit',
});

// ButtonVariant enum used by the Button variant prop
export const ButtonVariant = Object.freeze({
    DEFAULT: 'default',
    CLEAR: 'clear',
    PRIMARY: 'primary',
    ROUND: 'round',
});
